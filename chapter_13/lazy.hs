-- lazy.hs 
o = [1..] ++ undefined ++ [1..]
-- 因为++ 是右结合的，所以等价如下面
-- o = [1..] ++ (undefined ++ [1..])
-- 初看运行的时候会有exception出现
-- 不过由于haskell的lazy的特性
-- (x:xs) ++ ys = x : ( xs ++ ys ) 
-- 由于ys现在还不需要使用，所以
-- 没必要算出(undefined ++ [1..])的值
-- 所以不会产生异常
-- 同理
-- take 5 ([1..] ++ undefined ++ [1..])
-- 后面都不会动，可以正确快速取出前5个值
-- [1,2,3,4,5]
